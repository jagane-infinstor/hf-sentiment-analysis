name: docker-example

docker_env:
  image:  hf-sentiment-analysis-env

entry_points:
  main:
    parameters:
      input_dir: string
    command: "python sentiment_analysis.py --input_dir {input_dir}"
